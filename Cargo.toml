[package]
name = "cangjie"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Cangjie language support for Zed"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
